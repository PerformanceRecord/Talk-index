# AGENTS.md

このリポジトリでの実装ルール（短縮版）です。

- 変更は小さく分ける（1PR=1目的）。
- GitHub Actions 用の値は、ハードコードせず `secrets` を参照する。
- 環境変数は `strip()` して検証し、未設定時は明確なエラーメッセージを出す。
- 例外は握りつぶさず、原因がわかる文言にする。
- シート列やJSONキーを変更する時は、READMEも同時更新する。
- 新規ロジック追加時は、既存関数の責務を崩さず、サービス層に分離する。
